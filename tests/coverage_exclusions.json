{
  "comment": "Files under tests/ that intentionally contribute no tests to the CI 'not gpu' run. scripts/ci_coverage_gate.py fails on any test_*.py absent from the run and absent from this list, and on any entry here whose file no longer exists. Every exclusion states what runs the file instead, so 'not collected' can never quietly mean 'not covered'.",
  "excluded": {
    "tests/test_compile_core.py": "Standalone full-size compile benchmark with no test functions. Requires an otherwise idle GPU and is run deliberately by scripts/test_parallel.sh under RWKV_GPU_STRESS=1, not by pytest collection.",
    "tests/test_dmt_graph.py": "Standalone CUDA-graph parity program with no test functions. Same RWKV_GPU_STRESS=1 opt-in path as test_compile_core.py.",
    "tests/test_fused_optimizer_qualification.py": "Module-level pytest.mark.gpu: every test drives a CUDA device to compare the fused AdamW kernel against the foreach reference, so the suite is empty under 'not gpu' by design. NOT run by CI: the gpu job is gated on the TRAINVM_SELF_HOSTED repository variable, which is unset, so it reports 'skipping' on every pull request. The only thing that runs this is scripts/acceptance.sh --gpu on a real host, and --gpu is off by default.",
    "tests/test_loop_invariants.py": "Diagnostic module: defines a Core module and gate_mode_of helper, no test functions. It was named in the previous hand-maintained CI list and therefore contributed nothing while appearing covered.",
    "tests/test_loop_new_features.py": "Diagnostic module defining a Core module, no test functions. Also named in the previous hand-maintained CI list while contributing nothing.",
    "tests/test_megakernel.py": "Module-level pytest.mark.gpu; NOT run by CI: the gpu job is gated on the TRAINVM_SELF_HOSTED repository variable, which is unset, so it reports 'skipping' on every pull request. The only thing that runs this is scripts/acceptance.sh --gpu on a real host, and --gpu is off by default.",
    "tests/test_megakernel_linear.py": "Module-level pytest.mark.gpu; NOT run by CI: the gpu job is gated on the TRAINVM_SELF_HOSTED repository variable, which is unset, so it reports 'skipping' on every pull request. The only thing that runs this is scripts/acceptance.sh --gpu on a real host, and --gpu is off by default.",
    "tests/test_megakernel_multitoken.py": "Module-level pytest.mark.gpu; NOT run by CI: the gpu job is gated on the TRAINVM_SELF_HOSTED repository variable, which is unset, so it reports 'skipping' on every pull request. The only thing that runs this is scripts/acceptance.sh --gpu on a real host, and --gpu is off by default.",
    "tests/test_megakernel_ops.py": "Module-level pytest.mark.gpu; NOT run by CI: the gpu job is gated on the TRAINVM_SELF_HOSTED repository variable, which is unset, so it reports 'skipping' on every pull request. The only thing that runs this is scripts/acceptance.sh --gpu on a real host, and --gpu is off by default.",
    "tests/test_rosa_cuda.py": "Module-level pytest.mark.gpu; NOT run by CI: the gpu job is gated on the TRAINVM_SELF_HOSTED repository variable, which is unset, so it reports 'skipping' on every pull request. The only thing that runs this is scripts/acceptance.sh --gpu on a real host, and --gpu is off by default."
  }
}
